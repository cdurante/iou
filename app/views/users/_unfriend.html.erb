<%# Update this to work with whatever relationship model we build. Also make sure that the friend relationship is deleted from both users instead of just one (as was done with unfollowing). %>

<%= friendship = current_user.friendships.find_by_friend_id(@user.id) %>
(<%= link_to "remove", friendship, :method => :delete, class: "btn btn-large" %>)